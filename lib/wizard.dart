import 'package:flutter/material.dart';

class Wizard extends StatelessWidget {
  const Wizard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Wizard Page'),
      ),
    );
  }
}
